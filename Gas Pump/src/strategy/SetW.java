package strategy;

import data.DataStore;

public interface SetW {
    /* DESIGN PATTERN: Strategy - Strategy Interface */
    /* Responsibilities: Defines the interface for the 'Set W' algorithm */
    void setW(int w, DataStore data);
}
